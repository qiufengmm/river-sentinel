"""DS-8A 后端与智能体测试包。

说明：``ml/tests`` 下已有 ``test_config.py``，本目录加 ``__init__.py`` 后模块名变为
``backend.tests.test_config``，避免与 ``ml/tests/test_config.py`` 顶层模块名冲突，
从而支持 ``python -m pytest backend/tests ml/tests`` 一次跑通。
"""
