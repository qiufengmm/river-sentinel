# 数据目录

- `raw/`：本地不可变原始快照，不进入Git。
- `interim/`：清洗和对齐过程数据，不进入Git。
- `processed/`：可用于实验的处理后数据，不进入Git。
- `samples/`：小型合成样例或明确允许再分发的公开样例，可以进入Git。

每个真实数据源必须在 `docs/data-governance/` 记录来源、提供单位、访问条件、获取时间、字段字典和校验值。禁止将密钥、个人信息或未经授权的大规模数据提交到仓库。
