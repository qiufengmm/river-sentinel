# River Sentinel 项目协作规则

> 本文件是 `F:\code\engine` 的当前生效协作规则。项目面向浙江水利水电学院人工智能专业本科毕业设计，并兼顾相关课程实训成果复用。

## 1. 项目定位与目录

- 中文名称：河道水位预测与防汛决策智能体。
- 英文代号：River Sentinel。
- 项目根目录：`F:\code\engine`。
- 首期研究范围：温州市公开水雨情数据、5～20 个代表性河道站、未来 1/3/6 小时水位预测。
- 地区、数据源、站点集合和预测时域必须配置化，后续可以更换研究范围。
- 系统仅用于教学、科研与辅助分析，不替代水行政主管部门的正式监测、预警、调度或应急决策。

规划目录：

- `backend/`：FastAPI 业务接口、预测服务、规则引擎和 LangGraph 智能体；
- `frontend/`：Vue 3、TypeScript、Vite 和 ECharts 可视化系统；
- `ml/`：数据处理、特征工程、Scikit-learn、XGBoost 或 LightGBM、PyTorch 模型训练与评估；
- `data/`：数据来源说明、本地原始数据、中间数据、处理后数据和小型公开样例；
- `docs/`：需求、设计、实验、毕业设计过程材料、论文和答辩材料；
- `deploy/`：Docker Compose、PostgreSQL、Redis 和部署配置；
- `scripts/`：开发、验证、训练和文档辅助脚本。

开发期数据库允许使用 SQLite，部署环境使用 PostgreSQL；Redis 用于缓存和任务状态。不得为了展示技术栈而提前引入没有真实用途的基础设施。

## 2. 主 Agent 职责

主 Agent 负责需求分析、研究方案与系统架构设计、任务拆解、代码和实验审查、验证以及 Git 流程管理。

开始任务前必须：

1. 说明业务流程或研究流程；
2. 说明实现架构和关键文件；
3. 说明 API、数据库、数据集、特征、模型和文档影响；
4. 给出可执行的验收标准和验证命令；
5. 检查当前分支、工作树和已有未提交改动，明确本次任务边界。

主 Agent 不得在未说明影响和未安排审查的情况下直接修改核心业务代码、数据清洗规则、特征逻辑、训练逻辑、风险规则或论文实验结论。任务完成通知、工作树报告和审查摘要都只是线索，不能替代实际 `git diff`、代码、数据、实验产物和测试结果检查。

## 3. 标准工作流

1. 用户提出需求后，主 Agent 先说明业务或研究流程、实现架构、关键文件、接口、数据库、数据集与模型影响，以及可执行的验收标准。
2. 主 Agent 按真实边界拆分数据与算法、后端与智能体、前端与文档等任务，并分别生成可直接复制的详细提示词。简单任务不为拆分而拆分。
3. 用户将提示词发送给对应工作树对话；默认不通过 MCP 自动分发编码任务。每个工作树只能修改提示词明确授权的范围。
4. 用户回传工作树完成通知后，主 Agent 先读取 `.codex/reports/<task-slug>-report.md`，再检查实际 `git diff` 和 `git status`；报告与聊天摘要不能替代实际检查。
5. 主 Agent 根据任务影响调用相关只读角色：`project_explorer` 检查业务链路、模块依赖、接口和数据流；`ml_reviewer` 检查数据、模型、指标与实验结论；`code_reviewer` 检查正确性、安全性、异常处理和回归风险。没有相关影响的角色不必调用。
6. 发现问题时，主 Agent 输出包含文件位置、证据、风险、允许修改范围和验收要求的返工提示词，由用户转发给原工作树；返工后重新检查报告、diff 和必要的专项审查。
7. 审查通过后，运行相关单元测试、数据校验、训练烟雾测试、前端构建或接口验证，再按独立逻辑单元在功能分支创建中文 Git 提交。论文结论必须能对应数据快照、实验配置和代码版本。
8. 提交后确认功能 worktree 和 `main` 工作区都没有未提交改动，再由主 Agent 在主目录本地合并；不在功能 worktree 内切换到 `main`，不将未提交改动带入合并。
9. 本地合并完成后，在 `main` 重新运行本次改动的关键验证，并检查合并后的 diff、提交历史、实验索引和工作区状态。
10. 主 Agent 报告功能分支提交哈希、合并提交哈希、文件清单、数据与模型影响、验证结果和遗留问题；只有用户明确确认合并结果无误后，才执行 `git push`。

## 4. 工作树任务提示词与报告

发给工作树对话的提示词必须包含：

- 工作树绝对路径；
- 需求目标、业务流程或研究流程；
- 允许修改与禁止修改的文件范围；
- API、数据库、数据、特征、模型和文档约束；
- 异常处理、验收标准和验证命令；
- 禁止提交 Git、推送远程、合并分支或扩大任务范围；
- 完成后生成 `.codex/reports/<task-slug>-report.md`。

任务报告必须包含：

- 完成概述；
- 修改文件和关键位置；
- 业务、API 与配置影响；
- 数据来源、字段、特征、模型和实验影响；
- 实际运行的验证命令与完整结果摘要；
- 当前分支和 `git status`；
- 遗留问题、已知限制和后续建议。

报告是本地协作产物，默认不纳入功能提交。聊天中只需返回报告绝对路径、实现状态和阻塞情况；若无法生成报告，必须说明原因并在聊天中提供相同结构的完整结果。

## 5. Codex 协作角色

只配置以下三个只读角色。角色之间不重复启动；任务没有相关影响时不调用对应角色。

### `project_explorer`

- 定位代码、API、模块依赖、业务链路和数据流；
- 说明拟修改范围、上下游影响和已有实现证据；
- 返回文件路径、类或函数、接口、数据表或配置项等可核对依据；
- 不修改文件，不执行 Git 写操作，不提出与任务无关的大范围重构。

### `ml_reviewer`

- 检查数据来源、许可或开放条件、字段含义、质量、异常、缺失和时间对齐；
- 检查训练、验证、测试的时间顺序划分和潜在数据泄漏；
- 检查基线、模型、特征、指标、超参数、随机种子和可复现性；
- 检查实验结论能否由产物支持，是否遗漏失败实验、局限性或极端事件表现；
- 不修改数据、代码、模型和论文内容。

### `code_reviewer`

- 检查功能是否符合需求和验收标准；
- 检查正确性、安全性、参数校验、异常处理、接口一致性和回归风险；
- 检查是否误提交密钥、真实大数据、模型权重、缓存或构建产物；
- 只报告有证据的问题，按 P0、P1、P2 分级并给出文件路径、行号、复现条件和建议；
- 不修改文件，不执行 Git 写操作。

任何审查摘要均不能替代主 Agent 对实际 diff、数据、实验产物和验证结果的核对。

## 6. 数据与实验规范

- 每个数据源必须记录来源 URL、提供单位、许可或访问条件、获取时间、时间范围、字段字典和文件校验值；需要账号或接口申请时如实记录。
- `data/raw/` 中的原始数据视为不可变输入；清洗结果写入独立目录，禁止覆盖原始数据。
- 真实大规模数据不进入 Git；仓库仅允许提交来源明确、体量小且适合公开的小型样例。
- 训练集、验证集和测试集必须按时间先后划分；任何标准化、插补、特征统计或站点筛选都只能使用允许的数据范围拟合。
- 必须显式检查未来信息、目标泄漏、跨时间窗口重叠和不当的全量统计特征。
- 至少比较持久性或统计基线、机器学习模型以及 LSTM 或 TCN 等深度学习模型。
- 回归指标至少包含 MAE、RMSE、R² 和 NSE；超警事件至少报告准确率、召回率、F1 和预警提前量。
- 每次正式实验必须记录数据范围、站点集合、特征、参数、随机种子、代码提交、运行环境、开始与结束时间以及产物路径。
- 保留失败实验、误差分析和研究局限，不得只选择有利结果。
- 禁止伪造或修改原始数据、实验指标、图表、参考文献、系统日志和论文结论。

## 7. 智能体与防汛业务边界

- 风险等级必须由预测服务和确定性预警规则产生，大语言模型不得自由生成或修改风险等级。
- 智能体只能通过明确工具访问水雨情查询、历史分析、预测、规则判定、知识检索、图表和简报能力。
- 回答必须保留观测时间、数据新鲜度、模型版本、工具调用摘要和判断依据。
- 数据源失败时可以使用最近一次成功快照，但必须显著标注数据时间和陈旧状态，不得冒充实时数据。
- 正式模型不可用时只能回退到经过验证的基线模型，并记录回退原因和所用版本。
- 工具失败或证据不足时必须明确返回失败或不确定状态，不得补造数值、站点状态或防御建议。
- 页面、报告和智能体回答必须标注“教学科研辅助，不替代官方防汛决策”。
- 不得冒充水行政主管部门发布官方预警，不得在缺少真实水文条件和审批机制时提供可直接执行的调度指令。

## 8. 测试与验收

验证范围应与改动风险匹配：

- 后端：配置加载、健康检查、API 契约、业务服务、规则引擎和智能体工具测试；
- 算法：预处理、时间切分、特征、指标、训练烟雾测试和推理契约测试；
- 前端：类型检查、组件测试、关键交互测试和生产构建；
- 数据：字段、类型、时间范围、重复、缺失、异常、单位和来源一致性检查；
- 部署：Docker Compose 配置检查和环境变量完整性检查；
- 集成：使用小型公开样例完成从数据处理到预测、风险判定、智能体回答和前端展示的最小闭环。

宣称完成、修复、通过或可合并前，必须在当前改动上运行新鲜的验证命令并阅读完整结果；历史结果、预期结果和他人报告不能作为通过依据。合并到 `main` 后必须重新执行关键验证。

## 9. 毕业设计过程材料

项目开发必须同步维护以下材料：

- 选题审批；
- 任务书；
- 开题报告；
- 国内外文献综述；
- 外文文献与翻译；
- 指导与进度记录；
- 中期检查；
- 系统验收、测试报告和用户手册；
- 正式论文；
- 答辩 PPT、演示脚本和问题库；
- 最终归档清单。

仓库模板只提供过程支撑。论文模板、提交日期、正文字数、查重阈值和归档形式以当届计算机科学与技术学院及指导教师的最新通知为准；发现冲突时以最新正式通知为准并更新仓库记录。

## 10. Git 与文件安全

- 较大的数据、算法、后端、智能体、前端和论文阶段从最新 `main` 创建独立功能分支与 worktree；不混用工作树。
- 工作树实现阶段不得执行 `git commit`、`git push` 或 `git merge`，由主 Agent 在审查和验证后统一处理。
- 使用中文提交信息；每个独立功能或清晰逻辑单元创建一个提交。
- 合并前确认功能 worktree 与 `main` 均干净；合并后重新验证并检查提交历史。
- 未经用户明确确认，不执行 `git push`；推送前确认具体远程仓库和目标分支。
- 禁止提交 `.env`、密钥、Token、本地数据库、数据库备份、真实大规模数据、模型权重、训练缓存、`node_modules/`、`target/`、`dist/`、临时论文文件或个人信息。
- 禁止批量删除文件或目录，不得使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse` 或 `rm -rf`。
- 删除文件时一次只能处理一个经过确认的明确路径；如需批量删除，停止操作并请求用户手动处理。
- 不执行 `git reset --hard` 或丢弃用户改动的命令，除非用户对准确目标给予明确授权。

## 11. 汇报要求

阶段性和最终汇报必须包含：

- 当前分支、功能分支提交哈希和合并提交哈希；
- 新增、修改和删除的文件清单；
- 业务、API、数据库、配置、数据、特征、模型和论文材料影响；
- 实际运行的验证命令、结果和未运行项；
- 已知限制、遗留问题、风险和下一步可选方案。

未经实际 diff、数据与实验产物检查以及相关验证，不得宣称任务完成。审查角色的“通过”、工作树报告或聊天回执均不能单独作为完成依据。
