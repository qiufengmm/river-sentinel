# 数据源治理记录：温州市公共数据开放平台 `iid=6352` 温州水文河道水情信息

> 本文只记录来源、字段口径、访问状态与校验值存放位置，不记录任何凭据取值。
> 本系统仅用于教学科研与辅助分析，不替代水行政主管部门的正式监测、预警、调度或应急决策。
> **重要**：本数据集与 `cata_12720`（目标水位序列）的**同一监测断面尚未被证明**。在证明测站身份、水位基面与单位一致之前，**严禁与 `cata_12720` 拼接**（不得生成任何合并表、合并序列或合并统计）。

## 1. 数据集标识

| 项 | 值 |
| --- | --- |
| 数据集名称 | 温州水文河道水情信息（页面原文标题） |
| 数据集 ID / 标识 | `6352`（URL 中 `iid=` 后的值） |
| 接口目录 | 页面仅标注「数据接口：API」，未给出接口目录名，**待确认**（不得假定 `cata_` 前缀） |
| 来源页面 | <https://data.wenzhou.gov.cn/jdop_front/detail/data.do?iid=6352> |
| 提供单位 | 温州市水利局（页面「数源单位」原文）；数源单位地址「温州市绣山路198号」，联系方式 `0577-88222252` |
| 覆盖区域 | 页面「地理范围：全市」；摘要原文「包含合并测站编码,测站编码,时间,水位等信息」；与飞云江文成县段目标断面的关系**待确认** |
| 主题类别 | `water_level` |
| 检索关键词 | 温州水文河道水情信息 / 水文 河道 水位（候选源登记追溯） |
| 登记日期 | 2026-09-21 |

页面关键标注（原文照抄，2026-09-21 访问）：

- 数据量：**1922000**；时间范围：**2015年-2019年**；更新周期：每年；更新日期：2024-01-14；发布日期：2021-11-09。
- 数据分级：**完全开放**；开放条件：**开放网站**；访问/下载次数：49787/4288。
- 数据下载：XLS CSV XML JSON RDF；历史数据下载：**暂无历史数据**。
- 友情提醒（原文）：「为防止文件过大，直接下载的数据集文件仅包含800000条数据，请通过API接口方式获取全量数据。」

## 2. 接口与字段

接口基础路径：待确认（页面仅标注「数据接口：API」，未给出具体路径）。

文件下载端点（同 `cata_12720` 机制，`resource/data/download.do?fileType=<格式>&iid=6352`），已实测 CSV：HTTP 200，`Content-Disposition` 文件名 `cata_6352_csv.zip`。

字段字典（沿用平台官方字段名，本仓库不做重命名）：

| 字段（中文表头） | 英文名称 | 平台描述原文 | 数据类型 | 实测样例 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 合并测站编码 | `MGSTCD` | 合并测站编码（系统标识_原测站编码） | 字符串 | `SWZ_01425` | 前缀疑为系统标识（实测样本全部为 `SWZ_`），含义**待确认** |
| 测站编码 | `STCD` | 测站编码 | 字符串 | `01425` | 站身份字段；与目标断面的对应关系**待确认** |
| 时间 | `TM` | 时间 | 字符串 | `2019-09-09 10:25:00` | 格式 `YYYY-MM-DD HH:MM:SS`（**无毫秒**，与 `cata_12720` 的毫秒格式不同） |
| 水位 | `Z` | 水位 | 数值字符串 | `17.34` | **单位与水位基面均未标注，待确认** |

口径警示：

- 本数据集**有**测站身份字段（`STCD`/`MGSTCD`），而 `cata_12720` **没有**任何测站编码、测站名称或经纬度字段——两源之间**不存在可核对的站身份键**。
- 实测 `Z` 值范围 `-1.87` ~ `779.51`：跨 263 个测站混合后出现极大值，说明该字段在不同测站可能使用不同单位或水位基面，**单位与基面待确认**，短期内不应跨站比较。

## 3. 分页与配额

- 文件下载：**单次上限 800000 条**（页面友情提醒原文，2026-09-21 访问）；实测下载确为 **800000 行**。
- 全量数据（页面标注 1922000 条）需通过 API 获取；本仓库当前 **API 凭据未获批（`API_AUTH_PENDING`）** → 全量获取 **BLOCKED**（见第 4 节）。
- 迭代规则、单页上限（`MAX_PAGE_SIZE = 200` 既有契约是否适用）：待确认。

## 4. 许可与访问审批状态

- 开放类型：无条件开放（页面「数据分级：完全开放」，开放条件「开放网站」）。
- 许可协议：页面未标注具体许可协议名称，待确认。
- 当前状态：**文件下载通道可用（已实测）**；**全量 1922000 条经 API 获取：`BLOCKED`**（API 凭据 `WENZHOU_DATA_APPSECRET` 未获批，见 `source-cata-12720.md` 第 4 节；证据：2026-09-21 页面友情提醒原文「请通过API接口方式获取全量数据」）。
- 是否允许用于论文实验：可作为「辅助数据 / 迁移学习候选 / 测站身份核验来源」用于教学科研；**不得冒充 `cata_12720` 目标断面的历史延长数据**，也不得据此扩大目标序列样本量结论。
- 凭据只通过环境变量提供，不写入任何文件、产物、日志或异常消息。

## 5. 获取时间与校验值的存放位置

- 快照：`data/raw/wenzhou/cata_6352/20260921T073439Z-893cb9b51b2b.zip`（**5,137,876 字节**，内含 **160 个分片 CSV** `cata_6352_1.csv` ~ `_160.csv`，GBK 编码；`data/raw/` 被 `.gitignore` 忽略，不进入提交）。
- 清单：同目录同名 `.manifest.json`，含 `source_name`、`source_uri`、`request_parameters`、`acquired_at`、`sha256`、`file_size_bytes`、`http_status`。
- SHA-256：`893cb9b51b2b3d6a6e3976267c189855029bdd6ddc7b1a41c2a3590fb12a4111`；获取时间（UTC）：2026-09-21T07:34:39Z。
- 该快照为**下载上限内的截断子集**，不代表 2015—2019 全量。

### 逐文件指纹与实测内容

| 项 | 实测值 |
| --- | --- |
| 记录数 | 800000（等于页面声明的单次下载上限） |
| 文件名/格式 | `cata_6352_csv.zip` → 160 个 GBK CSV 分片 |
| 文件大小 | 5,137,876 字节（外层 ZIP） |
| SHA-256 | `893cb9b51b2b3d6a6e3976267c189855029bdd6ddc7b1a41c2a3590fb12a4111` |
| 下载时间（UTC） | 2026-09-21T07:34:39Z |
| 字段 | `合并测站编码,测站编码,时间,水位`；样例 `SWZ_01425,01425,2019-09-09 10:25:00,2.61` |
| 时间范围 | `2015-08-19 10:20:00` ~ `2019-09-23 14:15:02` |
| 时间格式 | `YYYY-MM-DD HH:MM:SS`（整分秒，无毫秒） |
| 采样间隔 | 单站内主流间隔 **5 分钟**（实测 47179 个 5 分钟间隔，其次 10 分钟 4194 个、15 分钟 446 个）；整点对齐行 66329 / 800000 |
| 缺失情况 | 天数层面：跨度 2015-08-19 ~ 2019-09-23，但**仅 111 天有记录**；按年分布：2015 年 23222 行 / 94 天，2016 年 351 行 / 2 天，2019 年 776427 行 / 15 天。下载子集高度集中于 2019-09-10~09-20 等少数日（单日最高 66199 行） |
| 重复 | 未做全表 `STCD`+`TM` 重复率精确统计（跨 263 站），**待确认** |
| 单位 | 页面与文件均未标注，**待确认**（`水位` 实测范围 `-1.87` ~ `779.51`） |
| 测站数 | **263 个** `STCD`（与 263 个 `MGSTCD` 一一对应，样例 `5901`、`01425`、`01535`、`20002`） |

## 6. 时间覆盖与更新频率

- 平台声明：更新周期每年；更新日期 2024-01-14；时间范围 2015年-2019年（2026-09-21 访问）。
- 实测（下载子集）：如上，跨度为 2015-08-19 ~ 2019-09-23，但覆盖极不均衡（见第 5 节），**不能视为连续时间序列**。
- 与 `cata_12720`（2022-06-02 ~ 2022-10-09）的**重叠天数：0**。因此两者之间不存在可用于同断面核验的时间重叠窗口。

## 7. 测站身份信息

| 项 | 值 |
| --- | --- |
| 是否含测站编码字段 | **有**：`测站编码`（`STCD`）、`合并测站编码`（`MGSTCD`） |
| 是否含测站名称字段 | 无（页面字段表与文件表头均无测站名称） |
| 是否含经纬度字段 | 无 |
| 与目标断面（`cata_12720` 所属断面）的关系 | **待确认**——无可用于比对的键 |
| 与目标断面的距离（km） | 待确认 |
| 推荐时滞（步长） | 待确认（须由 MS-5 在训练集上估计后人工复核） |

### 与目标序列的可比性逐项判定（对照 `cata_12720`）

| 核对项 | `cata_12720`（目标） | `iid=6352`（本数据集） | 判定 |
| --- | --- | --- | --- |
| 监测断面 / 测站身份 | **无测站编码、无测站名称、无经纬度**（仅 `监测时间`/`实时水位`/`主键`） | 有 `STCD` / `MGSTCD`，但**无站名与坐标** | **无法比对 → 待确认** |
| 水位字段名 | `实时水位`（`up_water_level`） | `水位`（`Z`） | 命名体系不同，**待确认** |
| 单位 | 页面与文件均未标注 | 页面与文件均未标注 | **待确认**（且 `Z` 跨站范围 `-1.87`~`779.51`，疑多基面混合） |
| 水位基面 | 未标注 | 未标注 | **待确认**，不得假设同一基面 |
| 时间格式 | `YYYY-MM-DD HH:MM:SS.mmm`，间隔约 30 秒 | `YYYY-MM-DD HH:MM:SS`，间隔约 5 分钟 | 采样口径不同 |
| 时间重叠 | 2022-06-02 ~ 2022-10-09 | 2015-08-19 ~ 2019-09-23 | **重叠 0 天**，无法用同期取值交叉验证 |
| 数源单位 | 文成县水投公司 | 温州市水利局 | 不同提供单位，口径一致性**待确认** |

**结论：与 `cata_12720` 的关系判为「待确认」（当前按「非同站」处理）。** 依据：缺少可核对的站身份键、无时间重叠、单位与基面均未标注、提供单位不同。在补齐上述证据前，本数据集只能作为「辅助数据 / 迁移学习候选 / 测站身份核验来源」，**不参与目标序列延长**。

## 8. 待确认项清单

1. `STCD` / `MGSTCD` 与目标断面的对应关系（需站名或坐标来源，例如后续核实 `iid=8205` 等元数据来源）；
2. `水位`（`Z`）的单位与水位基面，以及跨 263 站是否统一；
3. 全量 1922000 条的获取（依赖 API 凭据获批，当前 `API_AUTH_PENDING` / `BLOCKED`）；
4. 2019-09-23 之后是否有更新批次，以及 2015—2019 与 2022 之间是否存在设备更换或基面变更（无任何页面或文件证据，**待确认**）；
5. 是否有 2019—2022 年度的空缺（**时间断层**：2019-09-23 → 2022-06-02，约 2 年 8 个月无数据）。

### 断层与基准检查（2026-09-21）

| 检查项 | 结论 |
| --- | --- |
| 时间断层 | **存在**：`iid=6352` 实测末条 2019-09-23，`cata_12720` 首条 2022-06-02，中间约 2 年 8 个月无任一侧数据覆盖 |
| 设备变更 | **待确认**（两数据集页面与文件均未给出设备或观测方式变更说明） |
| 水位基准（基面）变化 | **待确认**（两数据集均未标注基面；且本数据集 `Z` 跨站范围达 `-1.87`~`779.51`，疑非统一基面） |
| 提供单位变化 | 不同：文成县水投公司 vs 温州市水利局（页面原文），口径一致性未证明 |

## 9. 使用限制

- 仅限教学、科研与辅助分析；不得作为防汛调度、预警发布或应急决策依据。
- 不得冒充水行政主管部门发布官方预警。
- **禁止拼接**：在证明测站身份、单位与水位基面一致之前，禁止与 `cata_12720` 生成任何合并表、合并序列或合并统计；本数据集不得用于扩大目标序列的样本量结论。
