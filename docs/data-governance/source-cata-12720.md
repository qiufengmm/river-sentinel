# 数据源治理记录：温州市公共数据开放平台 `cata_12720`

> 本文只记录数据来源、字段口径、访问状态与校验值存放位置，不记录任何凭据取值。
> 本系统仅用于教学科研与辅助分析，不替代水行政主管部门的正式监测、预警、调度或应急决策。

## 1. 数据集标识

| 项 | 值 |
| --- | --- |
| 数据集名称 | 文成县飞云江二期治理工程水位信息 |
| 数据集 ID | `12720` |
| 接口目录 | `cata_12720` |
| 来源页面 | <https://data.wenzhou.gov.cn/jdop_front/detail/data.do?iid=12720> |
| 提供单位 | 温州市公共数据开放平台发布；页面标注的具体提供单位以平台页面为准，本仓库不另行推断 |
| 研究范围 | 飞云江（文成县段）二期治理工程相关断面的水位观测 |

## 2. 接口与字段

接口基础路径：`https://data.wenzhou.gov.cn/jdop_front/interfaces/cata_12720`

| 路径 | 用途 |
| --- | --- |
| `/get_total.do` | 返回平台声明的记录总数 |
| `/get_data.do` | 分页返回数据记录，参数 `pageNum`、`pageSize` |
| `/get_dataupdate_date.do` | 返回平台数据更新日期 |

字段字典（沿用平台官方字段名，本仓库不做重命名）：

| 字段 | 含义 | 下游处理 |
| --- | --- | --- |
| `time_d` | 观测时间（平台原始时间文本） | 由 `normalize_water_level_rows` 解析并本地化到 `Asia/Shanghai`；不可解析、歧义或不存在的本地时间记为 `invalid_timestamp_rows` |
| `up_water_level` | 水位值（源字段名称为“实时水位”） | 转 `float64`；缺失记 `missing_level_rows`，非有限值记 `invalid_level_rows` |
| `z_id` | 记录标识 | 转字符串后作为 `source_record_id`；重复者保留最后一次出现并记 `duplicate_rows`；缺失或不可用记入 `invalid_timestamp_rows` |

口径警示：源字段名称中的“实时水位”**只是字段名称**，不代表平台向本系统持续实时推送。任何历史数据演示都必须标注“历史回放 / 离线数据”。

## 3. 分页与配额

- 单页上限：`200`（`MAX_PAGE_SIZE`），超过即由适配器拒绝，不做静默截断式请求。
- 迭代规则：按 `get_total.do` 声明的总数分页取数；单页返回超过剩余数量时截断，绝不产出超过平台声明数量的记录。
- 空页提前结束迭代。

## 4. 访问审批状态

- 当前状态：**`API_AUTH_PENDING`**——接口凭据（`WENZHOU_DATA_APPSECRET`）尚未获批/未提供，本仓库未持有真实凭据。
- 文件下载通道：**实测可用**（页面标注「完全开放、开放条件：暂无」，未要求登录），实测详情见第 9 节；API 凭据状态不变。
- 凭据只通过环境变量提供，**不写入任何文件、产物、日志或异常消息**；产物中对凭据形态做了兜底脱敏。
- 状态未变更为已授权之前：不得声称使用了真实接口数据，不得把样例或合成数据表述为实测数据。

## 5. 获取时间与校验值的存放位置

每次取数都会写不可变原始快照与相邻清单（快照写在当次运行的 run 目录内，run 目录自包含）：

- 快照：`<run 目录>/wenzhou/<dataset_id>/YYYY/MM/DD/<UTC时间戳>-<sha256前12位>.ndjson`
- 清单：同目录同名 `.manifest.json`，含 `source_name`、`source_uri`、`dataset_id`、`acquired_at`（获取时间，UTC）、`request_parameters`、`record_count`、`sha256`
- 快照为合法 JSON（NDJSON，非有限数值写 `null`），校验值即清单中的 `sha256`，可用 `sha256sum`/`Get-FileHash` 复核。
- 快照写入使用排他创建：已存在的快照绝不被覆盖；run 目录名唯一（时间戳 + 同秒序号），因此快照路径唯一、同一批数据重复运行不会撞名。
- 每次运行保留当次抓取的原始快照，**不做跨 run 去重**；长期运行需自行清理旧 run 目录。

## 6. 更新频率警示

- 平台的实际更新频率以 `/get_dataupdate_date.do` 返回为准，本仓库不做假设。
- 重跑管线时必须重新记录获取时间与快照 SHA-256；不同批次的结论不得混用。

## 7. 本地样例

- `data/samples/water_level_sample.csv` 是**合成演示数据**，数值由本项目按小时序列人为构造，不是真实观测值。
- 该样例刻意保留三类质量缺陷（1 个重复 `z_id`、1 个缺失水位、1 个非法时间戳），仅用于自动化测试与管线演示。

## 8. 使用限制

- 仅限教学、科研与辅助分析；不得作为防汛调度、预警发布或应急决策依据。
- 不得冒充水行政主管部门发布官方预警。

## 9. 文件下载通道（2026-09-21 实测）

> 本节为 DS-1 任务（开放数据来源核实与登记）实测登记。所有事实均可溯源到本节给出的 URL、访问日期与响应原文；无法确认的字段一律写「待确认」。

### 9.1 详情页下载入口（页面原文）

- 详情页 URL：<https://data.wenzhou.gov.cn/jdop_front/detail/data.do?iid=12720>（访问日期 2026-09-21，HTTP 200）。
- 页面提供两组下载入口，每组格式均为 **XLS / CSV / XML / JSON / RDF**（未出现 XLSX）：
  - 「数据下载」：按钮触发 JavaScript `showKfxy('<格式>','12720')`；
  - 「历史数据下载」：按钮触发 JavaScript `showKfxyHistory('<格式>','12720')`。
- 另有「数据预览」入口：`data_preview.do?iid=12720`（未实测展开）。
- 页面标注：数据量 **107105**；时间范围「2022年至今」；更新周期「每年」；更新日期 2026-05-12；发布日期 2022-10-20；数据分级「**完全开放**」，开放条件「暂无」，未出现「需登录」「需申请」字样；数源单位「文成县水投公司」。

### 9.2 实测下载（CSV 格式）

- 实测下载 URL：`https://data.wenzhou.gov.cn/jdop_front/resource/data/download.do?fileType=csv&iid=12720`（来自「数据下载」组入口的链接 `../resource/data/download.do?fileType=csv&iid=12720`）。
- 下载时间（UTC）：2026-09-21T06:48:43Z；HTTP 200；`Content-Type: application/octet-stream`；`Content-Disposition` 给出文件名 `cata_12720_csv.zip`；文件大小 **617016 字节**。
- SHA-256：`aa1fbc21e14063272539d324683c1b099faeffb5d15279bc34429dd9a42caddc`。
- 本地快照（排他创建，不进入 Git）：`data/raw/wenzhou/cata_12720/20260921T064843Z-aa1fbc21e140.zip`，同名 `.manifest.json` 含来源、参数、获取时间、记录数与校验值。
- 返回体为 ZIP 压缩包，内含 **22 个分片 CSV**（`cata_12720_1.csv` ~ `cata_12720_22.csv`，每片约 5000 行，编码 GBK）。「历史数据下载」入口（`showKfxyHistory`）未另行实测；本次「数据下载」返回的记录数已与页面标注数据量完全一致（见 9.3），两者是否同一数据全集**待确认**。

### 9.3 文件内容实测

- 字段名（中文表头，与平台字段表 `time_d` / `up_water_level` / `z_id` 对应）：`监测时间`、`实时水位`、`主键`。首行样例：`2022-08-29 15:44:46.000, 7.04, 771`。
- **测站编码/名称/经纬度字段：页面与文件均未出现**（页面摘要提到「测站名称」，但页面字段表与文件表头均无该字段，两处不一致，测站身份**待确认**）。
- 实际数据行数：**107105**，与页面标注数据量 107105 完全一致。
- 时间字段格式：`YYYY-MM-DD HH:MM:SS.mmm`（毫秒精度）；实际时间范围 **2022-06-02 18:33:45.000 ~ 2022-10-09 11:26:50.000**。页面标注时间范围「2022年至今」与实际末条记录 2022-10-09 不一致，**待确认**。
- `主键`（`z_id`）：107095 个唯一值，**10 条重复**。
- `实时水位`：无空值、无非数值；实测 min 0.0 / max 8.4 / mean 5.965；**恰为 0.0 的记录 5469 条（5.1%）**，是否为缺测哨兵值**待确认**。
- 异常行：1 行时间仅含日期无时刻（`2022-06-08, 5.98, 80824`）。

### 9.4 每日记录数分布（`expected_observations_per_day` / `min_daily_coverage` 实测依据）

- 覆盖跨度 130 天（2022-06-02 ~ 2022-10-09），有记录日 118 天，**完全缺失 12 天**：2022-07-07、2022-07-08、2022-09-03、2022-09-13、2022-09-14、2022-09-20、2022-09-21、2022-09-23、2022-09-26、2022-09-27、2022-09-29、2022-10-05。
- 每日记录数：**min 5 / 中位数 921 / max 3275**；仅 7 / 107105 行落在整点（`分:秒 = 00:00`），主流相邻记录间隔 30~31 秒（另含 985 条 0 秒间隔与 1866 条 1 秒间隔的近重复记录），属**秒级非均匀采样**，不存在固定小时采样率。
- 小时分布：00~23 时均有记录，05~10 时记录数明显偏少（最少 06 时 1926 条）。
- **建议（非正式口径）**：因采样为秒级非均匀，`expected_observations_per_day` 不宜按「固定 24 条小时记录」设定；宜以「每日有效记录条数下限」刻画覆盖（例如参照中位数与缺失日分布分级设定 `min_daily_coverage`）。正式阈值由 DS-2 基于更长实测数据与日聚合口径确定，本节只给事实与建议理由。
## 10. 历史数据下载通道核实（2026-09-21）

> 本节由 DS-1b 任务新增；合并后按时间顺序重编号为**第 10 节**，与第 9 节并存，两节内容均完整保留、不得删改任一节。
> 本节所有事实可溯源到 URL + 访问日期 + 页面或响应原文；无法确认的一律写「待确认」。

### 10.1 两个下载入口的实际机制（页面证据）

- 详情页（<https://data.wenzhou.gov.cn/jdop_front/detail/data.do?iid=12720>，2026-09-21 访问，HTTP 200）「数据下载」按钮调用 JS `showKfxy(type,id)`，「历史数据下载」按钮调用 JS `showKfxyHistory(type,id)`。
- 两函数定义在 <https://data.wenzhou.gov.cn/jdop_front/resources/jdop/common/base.js>（2026-09-21 访问），分别打开开放协议对话框：`../channal/kfxy.do?type=<格式>&id=12720` 与 `../channal/kfxy_history.do?type=<格式>&id=12720&history=1`。
- 对话框页面内的下载函数 `download(type,id)`（原文）显示真实下载端点：
  - 数据下载：`window.location.href = '../resource/data/download.do?fileType='+type+'&iid='+id`
  - 历史数据下载：`window.location.href = '../resource/data/download.do?fileType='+type+'&iid='+id+'&history=1'`
- 结论：两组入口**唯一差别**是 `&history=1` 参数；页面上不存在年包、月包、分卷选择或其它下载入口。

### 10.2 穷举矩阵（2 入口 × 5 格式 = 10 个组合，2026-09-21 实测，全部 HTTP 200）

基础 URL：`https://data.wenzhou.gov.cn/jdop_front/resource/data/download.do`

| 入口 | 格式 | 请求参数 | 字节数 | SHA-256（完整） | 包内记录数 |
| --- | --- | --- | --- | --- | --- |
| 数据下载 | CSV | `fileType=csv&iid=12720` | 617016 | `aa1fbc21e14063272539d324683c1b099faeffb5d15279bc34429dd9a42caddc` | 107105（22 分片） |
| 数据下载 | XLS | `fileType=xls&iid=12720` | 2287457 | `509f8230c01f8c2871563a93fc9740172a4fb8cdf9f484e16d462cbcc88bc3ac` | 22 分片，行计数**待确认**（XLS 二进制，本地无 XLS 解析器） |
| 数据下载 | XML | `fileType=xml&iid=12720` | 761702 | `8d5fb6ea8d19130d86ef4e2dedd6f652cf27d04ea0582398e8f38c796ce3dbde` | 107105（22 分片，按 `<time_d>` 计数） |
| 数据下载 | JSON | `fileType=json&iid=12720` | 653883 | `2f942c67b2e4b9b521b32253527364cc887eff7584d49c26b5c804ff626b62dc` | 107105（22 分片） |
| 数据下载 | RDF | `fileType=rdf&iid=12720` | 854195 | `2d934512f81a55b1ca7331704feca537ead925ba90e72f6de6d59a3548c1124c` | 107105（22 分片，按 `<rdf:Description` 计数） |
| 历史数据下载 | CSV | `fileType=csv&iid=12720&history=1` | 253 | `5751238bafa711c7166eb06a0e2f735de1541e0bc27a67c18dc2c8fca2799be0` | **10**（单文件 `cata_12720.csv`，366 字节） |
| 历史数据下载 | XLS | `fileType=xls&iid=12720&history=1` | 1297 | `2cf559581ee0b6b36dd0dffe117f2d7e763349c2d6261eb104afa6a1d2bc128a` | 10（同内容 XLS，13824 字节，按同类文件推定，行计数**待确认**） |
| 历史数据下载 | XML | `fileType=xml&iid=12720&history=1` | 321 | `bd52937072d58540c094b11161ec3f52cb04ccbd0c04a4542607c04be1285d24` | **10**（1091 字节） |
| 历史数据下载 | JSON | `fileType=json&iid=12720&history=1` | 275 | `e235e2f7618b0484d232c474e30dc84b13bf63124f7b38c57ba725e08eb2495f` | **10**（452 字节） |
| 历史数据下载 | RDF | `fileType=rdf&iid=12720&history=1` | 447 | `4e44fd5214d4a4a98e6ecf147e53b7e878b3ec0282b482cde531f8d1963d34f6` | **10**（2716 字节） |

- 「数据下载」5 种格式的 CSV/XML/JSON/RDF 记录数**均为 107105**，与页面标注数据量一致；时间范围均起于 `2022-08-29 15:44:46.000`（各包首条，全量最小时间为 `2022-06-02 18:33:45.000`）止于 `2022-10-09 11:26:50.000`。
- 「历史数据下载」5 种格式均只含 **10 条记录**：`监测时间` 范围 `2022-08-30 12:44:20.000` ~ `2022-08-30 13:02:56.000`，`主键`（`z_id`）386~395，字段与全量一致（`监测时间,实时水位,主键`）。
- **子集校验**：这 10 条记录逐行比对**全部属于全量 107105 条之内**（重合 10 / 10），说明历史下载返回的是**最新一小段快照**，而不是额外的历史数据。

### 10.3 结论：是否存在更多数据

**否（已穷举入口与格式，清单见 10.2）**。依据：

1. 页面上只有「数据下载」与「历史数据下载」两组入口，无年包/月包/分卷入口；
2. 「历史数据下载」实测只返回 10 条且为全量子集；
3. 「数据下载」5 种格式均返回同一规模 107105 条，与页面标注数据量一致；
4. CSV 本次 SHA-256 与 DS-1（2026-09-21T06:48:43Z）下载结果**完全相同**，说明返回内容确定、非随机截断。

因此：**107105 行 / 118 个有记录日 / 2022-06-02 ~ 2022-10-09 是该数据集本身的现有规模**，不是下载截断或解析遗漏。页面标注「时间范围：2022年至今」与实际末条 2022-10-09 不一致，**待确认**（不排除平台后续更新或不再更新）。

### 10.4 尺度相关实测（DS-1b 计算，仅证据，非正式口径）

基于清洗后数据（去除 10 条重复 `z_id`、1 条无时刻记录，保留 107094 条）：

- 日尺度：有记录 **118 天** / 跨度 130 天，缺失 12 天，**最长连续有记录天段 57 天**；严格相邻可用样本 D+1 109、D+2 102、D+3 96；按时间顺序 7:1.5:1.5 划分可得训练 82 / 验证 17 / 测试 19 天，其中测试段（起于 2022-09-12）内 D+1 12、D+2 11、D+3 12，三者同时成立仅 5 天。
- 小时尺度：有记录 **2026 小时** / 跨度 3090 小时，空缺 1064 小时，**最长连续段 720 小时（30 天）**；每小时内记录数 min 1 / 中位 39 / max 169；可用监督样本（源与目标小时均有记录）步长 1 小时 1950、3 小时 1802、6 小时 1610；测试段可用样本（后 15%）约 144 / 118 / 84。
- 6 小时尺度：有记录 394 块 / 跨度 515 块。
